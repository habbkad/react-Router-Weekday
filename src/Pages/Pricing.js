import React from "react";
import { Link } from "react-router-dom";

const Pricing = () => {
  return (
    <div>
      <h1>Pricing is a {<Link to={"/"}>boy</Link>}</h1>
    </div>
  );
};

export default Pricing;
